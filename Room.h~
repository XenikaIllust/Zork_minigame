#ifndef ROOM_H
#define ROOM_H

#include "Border.h"

#include <iostream>
#include <vector>
#include <string>

//#include "rapidxml-1.13/rapidxml.hpp"
//#include "rapidxml-1.13/rapidxml_print.hpp"

using namespace std;
//using namespace rapidxml;

class Room{

private:
	string name;
	string description;
	string type;

public:
	Border* north;
	Border* south;
	Border* west;
	Border* east;

public:
	Room(string, string)
//	Room(string, string, xml_node<>*);
	Room(string, string, string, string);
	virtual ~Room();

	void setBorder(string, string);
	void setType(string);

	string getRoomName();
	string getRoomDescription();
	string getType();	
};

#endif
